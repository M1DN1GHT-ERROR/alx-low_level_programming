#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int _putchar(*phr)
{
	char *phr = "_putchar";

	while (*phr)
	{
		_putchar(*phr);
		str++;
	}
	_putchar('\n');

	return (0);
}
