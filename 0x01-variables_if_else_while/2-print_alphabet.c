#include <stdio.h>

/**
 * main - prints the alphabet in lowercase,
 *
 * Return: 0 (success)
 *
 */

int main(void)
{
	ch letter;
	for (letter = 'a'; letter <= 'z'; ch++)
	{
		putchar(letter);
	}
	putchar('\n')
		return (0);
}
